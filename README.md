# Playwright E2E Framework

## What this is
End-to-end UI automation framework using Playwright + TypeScript, designed to run locally in Jenkins CI.


## What it covers
-   Smoke tests (@smoke)
-   Regression tests (@regression)
-   HTML + JUnit reporting
-   CI execution via Jenkins pipeline
